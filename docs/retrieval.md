# 检索 retrieval

检索就是"拿问题向量在知识库的向量里找最近的几个"。流程：问题先经过同一个
编码模型变成向量，再与库里每个块的向量算余弦相似度，按得分从高到低排序，
取前 top-k 个块作为参考资料。

top-k 是精度和成本的权衡：k 太小容易漏掉正确资料；k 太大会把不相关段落也塞
进提示词，既浪费 token 又可能干扰模型作答。从零实现时，一次矩阵乘法加一次
排序就足够了，效果好坏主要取决于向量化模型的质量。
