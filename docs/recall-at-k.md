# recall@k 评测

recall@k 用来回答一个问题：正确答案有没有被检索到。做法是给每个测试问题标注
它应该命中的文档（金标准），跑检索取 top-k 个块，看这些块的来源文档里是否
包含金标准文档，命中记 1 分，最后除以问题总数得到指标。

k 越大越容易命中，所以汇报时必须带上 k。如果 recall@3 = 100%，说明前三名里
必有正确资料；如果只有把 k 放大到 8 才能命中，说明排序质量不行，应该考虑
换编码模型，或者在检索之后加重排（rerank）。
