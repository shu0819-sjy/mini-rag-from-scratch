# 什么是 RAG

RAG（Retrieval-Augmented Generation，检索增强生成）= 先检索、再生成。把用户的
问题先拿去知识库里捞出最相关的几段资料，再把资料连同问题一起交给大模型回答。

这样做有三个好处：第一，知识库可以随时增删更新，不需要重新训练模型；第二，
答案有据可查，可以把引用到的原文段落一并展示，方便人工核对；第三，能明显减少
大模型一本正经胡说八道（幻觉）的概率，因为它被要求"依据检索到的资料回答"。
