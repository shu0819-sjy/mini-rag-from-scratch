# 向量化 embedding

embedding 是把一段文本映射成一个固定长度的向量（比如 384 维），让"语义相近的
文本对应空间里相近的向量"。编码模型在海量语料上预训练过，学会了把同义不同词
的句子摆到相近的位置。

于是"怎么退款"和"退货流程是什么"虽然几乎没有共同词，向量距离却很近，检索时
都能命中同一段售后说明。向量化质量直接决定检索效果的上限：模型选错了领域或
语种，后面再怎么调参数都是白费功夫。
