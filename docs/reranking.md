# 重排 reranking

重排是在粗检索之后的精细排序：先用向量检索快速捞出 top-50 候选，再用更强的
模型（比如交叉编码器）把问题和每个候选段落成对打分，重新排出最终的 top-5。

什么时候需要它：当向量检索的排序质量不够好、正确段落总在 top-10 里却挤不进
top-3 时，加一个重排器通常有立竿见影的提升。代价是速度变慢，所以只对少量
候选精算，粗检索仍然交给轻快的向量检索来完成。
