# 余弦相似度

余弦相似度衡量两个向量方向的接近程度：夹角越小越相似，取值范围是 [-1, 1]，
1 表示方向完全相同，0 表示正交（不相关），-1 表示方向完全相反。它只看方向
不看长度，所以文本长短不同也能公平比较。

实际计算时先把向量归一化成单位长度，这样余弦相似度就等于两个向量的点积。
于是一个问题与全部文本块的相似度，只需要一次矩阵乘法就能全部算出来，纯
numpy 也能跑到瞬间完成，完全用不上专门的向量数据库。
