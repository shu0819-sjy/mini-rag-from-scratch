# 分块 chunking

切块是把长文档拆成小段的过程。编码模型的输入长度有限，长文档也没法塞进一个
向量里，所以必须先切。常见策略：固定长度切、按句子或段落切、按标题层级切。

块与块之间要保留一定的重叠（overlap）。如果答案句子恰好横跨两块边界，不重叠
就会两边都只剩半句话，两边都检索不准。重叠取块长度的 10%~20% 为宜：太大则
存储翻倍，重复内容还会挤占 top-k 的名额，把别的资料挤出去。
